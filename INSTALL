     Basic perl module install:
            gunzip -c enum-1.010.tar.gz | tar -xvf -
            cd enum-1.010
            perl Makefile.pl
            make
            make test
            make install

